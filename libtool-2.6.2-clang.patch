--- origsrc/libtool-2.6.2/m4/libtool.m4
+++ src/libtool-2.6.2/m4/libtool.m4
@@ -7865,10 +7865,11 @@
     output_verbose_link_cmd=
   fi
 
+  eval std_shrext=\"$shrext_cmds\"
   for p in `eval "$output_verbose_link_cmd"`; do
     case $prev$p in
 
-    -L* | -R* | -l* | */libclang_rt.*.a)
+    -L* | -R* | -l* | */libclang_rt.*.a | *.${libext} | *${std_shrext})
        # Some compilers place space between "-{L,R,l}" and the path.
        # Remove the space.
        if test x-L = x"$p" ||

